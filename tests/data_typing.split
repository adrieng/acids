$$
${
(*
        run_test:
        [
                Good: "asc -check"
        ]
*)
$}

let node f () = 0

let node g () = x where rec x = 0

$$

let node f () = 0

let node g () = x where rec x = 0

let node k () = g ()

$$

let node f () = 0 + x where rec x = 1

$$

let node f () = (x, y) where rec x = y and y = x

$$

let node f () = o where
    rec o = merge <1(0)=1> 0 (buffer o)

$$

let node f () = o where
    rec o = merge <1(0)> with
                  | 0 -> 0
                  | 1 -> buffer o
            end

$$

let node f () = <('012345')>